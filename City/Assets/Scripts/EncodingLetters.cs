using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum EncodingLetters
{
    unknown = '1',
    save = '[',
    load = ']',
    draw = 'F',
    draw2 = 'G',
    turnRight = '+',
    turnLeft = '-'
}
